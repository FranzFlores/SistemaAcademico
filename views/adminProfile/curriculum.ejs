<%-include('../header')%>
<div class="row">
    <div class="col-md-4">
        <div class="card">
            <h4 class="center-align title-form">Mallas Curriculares</h4>
            <div class="card-content">
                <%- locals.flash %>
                <form action="/curriculum/create" method="POST" id="curriculumForm">
                    <div class="row">
                        <div class="input-field col s12">
                            <input id="year" type="text" name="year" class="validate" required>
                            <label for="year">Año Malla Curricular</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-field col s12">
                            <select name="career" id="careers">
                            </select>
                            <label for="career">Carrera</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-field col s12">
                            <select name="numCycles" id="numCycles">
                                <% for(var i=8;i<=12;i++){ %>
                                <option value="<%=i%>"><%= i%></option>
                                <% } %>
                            </select>
                            <label for="numCycles">Número de Ciclos</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-field col s12">
                            <select name="timeCycle" id="timeCycle">
                                <% for(var i=16;i<=20;i++){ %>
                                <option value="<%=i%>"><%= i %></option>
                                <% } %>
                            </select>
                            <label for="timeCycle">Duración del Ciclo(Semanas)</label>
                        </div>
                    </div>
                    <div class="card-action">
                        <div class="input-field col-s12 center-align">
                            <button id="button" class="btn">Guardar</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="col s8">
        <table class="highlight centered responsive-table">
            <thead>
                <tr>
                    <th>Carrera</th>
                    <th>Año Malla Curricular</th>
                    <th>Número de Períodos</th>
                    <th>Duración por Período</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% curriculums.forEach(function(curriculum){ %>
                <tr>
                    <td><%=curriculum.career.name%></td>
                    <td><%=curriculum.year%></td>
                    <td><%=curriculum.numCycles%> períodos</td>
                    <td><%=curriculum.timeCycle%> semanas</td>
                    <td>
                        <a class="edit" data-id="<%=curriculum._id%>">
                            <i class="material-icons btn">edit</i>
                        </a>
                        <a class="delete" data-id="<%=curriculum._id%>">
                            <i class="material-icons btn">delete</i>
                        </a>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</div>
</div>


<%-include('../footer')%>
<script src="/js/views/curriculum.js"></script>